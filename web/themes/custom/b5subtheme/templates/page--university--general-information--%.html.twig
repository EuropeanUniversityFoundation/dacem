

{{dump(university)}}

<div class="university-banner">
  {% if university.field_picture is not empty %}
    {% set media_entity = university.field_picture.entity %}
    {% if media_entity is not empty %}
      {% set file_entity = media_entity.field_media_image.entity %}
      {% if file_entity is not empty %}
        <div class="university-photo">
          <img src="{{ file_url(file_entity.uri.value) }}" alt="{{ university.title.value }} " class="university-img">
        </div>
      {% endif %}
    {% endif %}
  {% else %}
    <p>No hay imagen disponible.</p>
  {% endif %}
</div>

<div class="university-details">
  <!-- Mostrar el país (no traducible) -->
  {% if university.field_country is not empty %}
    <p><strong>País:</strong> {{ university.field_country.value }}</p>
  {% else %}
    <p><strong>País:</strong> No disponible</p>
  {% endif %}

  <!-- Mostrar la información -->
  {% set current_language = university.langcode.value %}
  {% if university.field_informacion[current_language] is not empty %}
    <div class="university-info">
      <strong>Información:</strong>
      <p>{{ university.field_informacion.value }}</p>
    </div>
  {% else %}
    <p><strong>Información:</strong> No disponible</p>
  {% endif %}

  <!-- Mostrar los requisitos de admisión -->
  {% if university.field_admission_requirements is not empty %}
    <div class="admission-requirements">
      <strong>Requisitos de Admisión:</strong>
      <p>{{ university.field_admission_requirements.value }}</p>
    </div>
  {% else %}
    <p><strong>Requisitos de Admisión:</strong> No disponible</p>
  {% endif %}
</div>


<!-- Cargar el bloque de autoridades académicas -->
{% set academic_authority_block = drupal_block('views_block:academic_authority-block_1') %}

{% if academic_authority_block is not empty %}
  <h2>El bloque fue cargado correctamente</h2>
  {{ academic_authority_block }}
{% else %}
  <h2>No se pudo cargar el bloque</h2>
{% endif %}
