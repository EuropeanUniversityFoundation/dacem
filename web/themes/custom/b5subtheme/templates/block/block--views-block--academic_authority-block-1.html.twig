{{ attach_library('b5subtheme/fontawesome') }}


{% if content['#view'].result|length > 1 %}

<div class="academic-authorities bg-light mt-5 mb-5" id='academic-authorities'>
	<br>
	<h2 style="font-weight: bold;">ACADEMIC AUTHORITIES</h2>

	


	<div class="container-fluid">
		<div class='my-custom-container-2'>

			<div class="authorities-grid">
				{% for row in content['#view'].result %}
					<div class="authority-item">
						{% set file_entity = row._entity.field_ac_photo.entity %}
						{% if file_entity is not empty %}
							<div class="authority-photo">
								<img src="{{ file_url(file_entity.uri.value) }}" alt="{{ row._entity.title.value }}">
							</div>
						{% endif %}
						<div class="authority-info">
							<h4>{{ row._entity.title.value }}</h4>
							{% if row._entity.field_institution_role is not empty %}
							<p>{{ row._entity.field_institution_role.value | upper}}</p>
							{% endif %}
							<div class="authority-icons mt-2">
								<a href="#" class="authority-icon me-3">
									<i class="fas fa-globe fa-lg"></i>
								</a>
								<a href="#" class="authority-icon me-3">
									<i class="fab fa-instagram fa-lg"></i>
								</a>
								<a href="#" class="authority-icon">
									<i class="fab fa-linkedin fa-lg"></i>
								</a>
							</div>
						</div>
					</div>

				{% endfor %}
			</div>
		</div>

	</div>
	<br>
</div>

{% endif %}
